configs = {
    ArenaSize : [500, 500],
    RobotSize : [20, 20],
    PoundRadius : 25,

    // For the general inactivity process
    Timestamp : Date.now(),
    DrawingOpt : {
        Enemy: true,
        Self: true,
        Text: true,
        DetectionResult: true
    }
}
